# 從 Notion 同步內容 → 建置 → 發佈到 GitHub Pages。
#
# 觸發時機：
#   1. 每小時整點自動跑一次（預約發布靠這個）
#   2. 你在 GitHub 網頁（手機也可以）按 Run workflow
#   3. 有人 push 到 main
#
# 前置設定（只要做一次）：
#   Settings → Secrets and variables → Actions 新增兩個 secret：
#     NOTION_TOKEN        Notion 整合的 API 金鑰
#     NOTION_DATABASE_ID  資料庫 ID
#   Settings → Pages → Source 設為「GitHub Actions」
name: 同步 Notion 並部署

on:
  schedule:
    # 每小時整點檢查一次。頻率要夠密，「預約發布」才有意義——
    # 預約 09:00 的文章，最晚 10:00 會上線。
    - cron: '0 * * * *'
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write   # 需要寫入權限，才能把同步結果 commit 回 repo
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  sync-build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 取出程式碼
        uses: actions/checkout@v4

      - name: 安裝 Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # 用 npm install 而非 npm ci：package-lock.json 是在 Windows 產生的，
      # 缺少 Linux 版的 rollup 原生套件，npm ci 會直接失敗。
      - name: 安裝相依套件
        run: npm install --no-audit --no-fund

      # --prune-all：CI 是全新的 checkout，直接清空後重建，
      # 產出就是 Notion 當下狀態的精確鏡像。
      - name: 從 Notion 同步越後飯
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
        run: npm run sync -- --prune-all

      - name: 從 Notion 同步富士搖滾
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_FRF_DATABASE_ID: 2f414f28e4568001b22ad297299115e8
          NOTION_FRF_DATA_SOURCE_ID: 2f414f28-e456-80cb-b742-000b2aeb130f
        run: npm run sync:frf -- --prune-all
        continue-on-error: true

      - name: 內容有變化就存回 repo
        run: |
          git config user.name "notion-sync[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A src/content/posts src/content/frf public/images/posts public/images/stamps public/images/frf-posts
          if git diff --staged --quiet; then
            echo "內容沒有變化，不需要 commit"
          else
            git commit -m "sync: 從 Notion 更新內容"
            git push
            echo "已更新 repo 內容"
          fi

      - name: 建置網站
        run: npm run build

      - name: 上傳建置結果
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: 發佈到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
