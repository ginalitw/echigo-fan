---
const { id } = Astro.params;
export function getStaticPaths() {
  return [{ params: { id: 'beginner' } }, { params: { id: 'returner' } }];
}
return Astro.redirect(id === 'returner' ? '/fujirock/upgrade/' : '/fujirock/', 301);
---
